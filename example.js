// Dit is een functie
let myPrint = function (param) {
  logger.debug("param = " + param);
};

// Lambda functie
let myFunction = (param) => {
  logger.debug("my param = " + param);
};

myFunction(5);
